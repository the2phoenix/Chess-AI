# --- Secrets / env ---
.env
.env.local
.env.*.local
*.key
serviceAccountKey.json
# Your real Supabase browser config (URL + anon key); commit the .example only.
web/supabase-config.js
# Per-user adaptive opponent models (Mode F3) — local user data, not source.
web/opponent_models/
# Continual-learning experience pool (Phase 8) — grows large; regenerated by
# pipeline.py collect / human play. The small deployed/ registry is kept.
training/pool/

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# --- Node / frontend ---
node_modules/
dist/
build/
.next/
.cache/
npm-debug.log*
yarn-error.log*

# --- Vercel ---
.vercel

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# --- Project artifacts (optional: keep small weights, ignore bulky logs) ---
training/logs/
training/checkpoints/
# per-run training dumps (run.json can be MBs) — regenerable, keep out of git
training/runs/
*.log

# Note: small evolved weights.json and recorded PGN are usually fine to commit.
# Large datasets or training dumps should stay out.
